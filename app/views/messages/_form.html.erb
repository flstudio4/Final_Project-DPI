<%= form_with model: @message, data: { turbo_frame: "new_message" }, id: 'new_message_form' do |form| %>
  <% if @message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@message.errors.count, "error") %> prohibited this message from being saved:</h2>

      <ul>
        <% @message.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.hidden_field :chat_id %>

  <div class="form-group">
    <%= form.text_area :content, rows: 5, class: "w-100"  %>
  </div>

  <div class="actions">
    <%= form.submit "Send message", class: "btn btn-outline-primary" %>
  </div>

<% end %>
