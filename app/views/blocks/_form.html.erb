<%= form_with(model: block) do |form| %>
  <% if block.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(block.errors.count, "error") %> prohibited this block from being saved:</h2>

      <ul>
        <% block.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :blocker_id, style: "display: block" %>
    <%= form.number_field :blocker_id %>
  </div>

  <div>
    <%= form.label :blocked_id, style: "display: block" %>
    <%= form.number_field :blocked_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
